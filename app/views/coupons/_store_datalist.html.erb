<%= f.text_field :store_name, placeholder: "Select Store or Create New", list: "store_names", class: "form-control" %>
  <datalist id="store_names">
    <% all_stores.each do |store| %>
      <option value="<%= store.name %>">
    <% end %>
  </datalist>